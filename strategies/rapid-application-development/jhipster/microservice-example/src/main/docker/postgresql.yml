version: '2'
services:
    petstore-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/petstore/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=petstore
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
