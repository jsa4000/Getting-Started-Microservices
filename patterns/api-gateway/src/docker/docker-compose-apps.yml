version: '3.5'

services:

  gateway:
    image: gateway:latest
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
    - 8080:8080
    networks:
    - gateway-network

  customer-service-01:
    image: customer:latest
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
    - 8081:8080
    networks:
    - gateway-network

  customer-service-02:
    image: customer:latest
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
    - 8082:8080
    networks:
    - gateway-network

  customer-service-03:
    image: customer:latest
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
    - 8083:8080
    networks:
    - gateway-network

networks:
  gateway-network:
    external:
      name: gateway-network